# Jenkins runs this playbook on itself (localhost connection).
# For ad-hoc runs against the Jenkins EC2 host over SSH, replace
# localhost with the instance private IP and set ansible_connection=ssh.
all:
  children:
    jenkins:
      hosts:
        localhost:
          ansible_connection: local
