apiVersion: v1
kind: Service
metadata:
  name: capstone-app
  namespace: capstone-app
  labels:
    app: capstone-app
spec:
  type: LoadBalancer
  selector:
    app: capstone-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
